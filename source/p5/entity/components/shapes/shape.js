import { Component } from "../component";

export class Shape extends Component {
  constructor() {
    super('shape');
  }

  setup(p5, systems) {
    super.setup(p5, systems);
  }

  setupComponents(components) {
    super.setupComponents(components)
  }

  display() {}
}